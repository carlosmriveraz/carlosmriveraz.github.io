const options = {
  strings: [
    'Fullstack developer',
    'Android developer',
    'Dev lover &#128153; &#128155; &#128153; fan',
  ],
  typeSpeed: 80,
  backSpeed: 50,
  backDelay: 1000,
  startDelay: 1000,
  loop: true,
};

const typed = new Typed('.animate-subheading', options);